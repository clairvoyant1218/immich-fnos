# 飞牛应用商店提交资料（v1.0）

## 应用信息

- 应用名称：Immich 相册
- 应用包名：`immich`
- 版本：`1.0`
- 维护者 / 分发者：`clairvoyant`
- 架构：`x86`
- Web 入口：`2283`

## 权限与数据说明

应用以 root 运行，因为需要读取用户指定的存储根和外接盘，并由应用中心管理 Docker Compose。默认挂载 `/vol1`、`/vol00`、`/vol01`、`/vol2`、`/vol3` 到容器的 `/storage*` 路径。应用数据位于 `${STORAGE_ROOT}/Docker/immich/`。

首次安装会在本机生成数据库密码。自动初始化会尝试建立 `Administrator`；若当前 Immich 版本拒绝空密码，会生成随机密码并只保存到数据目录下的 600 权限凭据文件。外部库路径可在 `.env` 中调整。

## 提交前必须补齐

1. 在干净的 fnOS x86 实例执行 `scripts/build-on-fnos.sh`，获得 `dist/immich-1.0.fpk` 和 `SHA256SUMS`。
2. 实测全新安装、首次启动、管理员登录、外部库扫描、升级和回滚。
3. 按商店后台要求补充应用截图、图标、隐私说明、支持联系方式和开发者签名/证书。
4. 确认商店允许第三方 root/Docker 应用，并按审核要求锁定上游镜像版本。
